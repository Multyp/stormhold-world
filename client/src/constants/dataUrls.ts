const dataUrls = {
  history: `${process.env.NEXT_PUBLIC_API_LINK}/history`,
  characters: `${process.env.NEXT_PUBLIC_API_LINK}/characters`,
  locations: "src/data/locations",
};

export default dataUrls;
